import { init, SDK_NAME, SDK_VERSION, log } from 'front-monitor-browser';
import { MonitorVue } from 'front-monitor-vue';
import { errorBoundaryReport } from 'front-monitor-react';
export { init, SDK_NAME, SDK_VERSION, MonitorVue, log, errorBoundaryReport };
